-- This timestamp was applied by an earlier generated migration that was later
-- replaced by 20260426081635_optimize_follower_visibility.  Keep a no-op
-- marker so Drizzle ORM 1.0 can upgrade existing migration tables that already
-- contain the older record.
SELECT 1;
